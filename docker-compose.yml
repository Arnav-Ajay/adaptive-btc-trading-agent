version: "3.9"

services:
  trading-agent:
    build: .
    container_name: adaptive-btc-trading-agent
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./config:/app/config
